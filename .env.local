# Local development environment variables (used by docker-compose.local.yml)
# This uses a standard PostgreSQL database instead of Neon Local

# Express / App
PORT=3000
LOG_LEVEL=debug
JWT_SECRET=dev-secret-change-me

# Database (Local PostgreSQL)
DATABASE_URL=postgres://postgres:dev_password@postgres:5432/acquisitions_dev

# Arcjet
ARCJET_KEY=ajkey_01k2emzfzafjv953zhhcr9e51v