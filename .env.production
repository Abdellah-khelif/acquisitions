# Production environment variables (example)
# Set these via your deployment platform's secret manager or CI/CD, not in source control.

NODE_ENV=production
PORT=3000
LOG_LEVEL=info

# Neon Cloud Postgres URL (serverless)
# Example: postgres://user:password@your-project.region.neon.tech/dbname?sslmode=require
DATABASE_URL=postgres://user:password@your-project.region.neon.tech/dbname?sslmode=require

# Strong JWT secret for production
JWT_SECRET=replace-with-a-strong-secret
